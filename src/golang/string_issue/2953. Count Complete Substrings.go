package string_issue

//func calc_countCompleteSubstrings(word string, k int) int {
//
//}

func countCompleteSubstrings(word string, k int) int {
	var l int = len(word)
	var res int = 0
	for left := 0; left < l; {
		var right int = left

		for right < l {
			if right == left {

			}
		}
	}

	return res
}
