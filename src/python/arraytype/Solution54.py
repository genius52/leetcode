class Solution:
    def spiralOrder(self, matrix: list[list[int]]) -> list[int]:
        rows = len(matrix)
        columns = len(matrix[0])
        dirs = [[0,1],[1,0],[0,-1],[-1,0]]
        direction = 0
        path = [int]


        return path
